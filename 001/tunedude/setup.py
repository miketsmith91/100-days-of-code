# TODO: ADD LICENSE, EMAIL, URL, AND A TON OF OTHER SHIT LATER

#TODO: proper installs for non python installs. For Mac installs this would include Xcode, libmagic, ffmpeg, and possibly a way
#to automate setting up a virtual environment automatically.

# for FFMPEG, need to automate command brew install ffmpeg